# Watcher IDS Dashboard — Configuration
# Edit then run: systemctl restart watcher
#
# Uncomment ONE WATCHER_ARGS line with all your options, e.g.:
#WATCHER_ARGS=--eve /var/log/suricata/eve.json --port 8765 --retain-days 90

# DeepSeek API key for AI-powered alert explanations (optional).
# Get a key at: https://platform.deepseek.com/api_keys
# You can also set or override this in Settings → AI Explain.
#DEEPSEEK_API_KEY=sk-...
